{
  "buildCommand": "rm -rf dist && mkdir dist && cp -r index.html privacy.html jatekszabalyzat.html sw.js css js assets icons manifest.json dist/",
  "outputDirectory": "dist",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
